name: boilerplate_generators
description: A starting point for Dart libraries or applications.
version: 1.0.0

environment:
  sdk: '>=2.14.0 <3.0.0'


dependencies:
  analyzer: ^2.5.0
  boilerplate_annotations: ^1.0.0
  build: ^2.1.1
  source_gen: ^1.1.1

dev_dependencies:
  build_runner: ^2.1.4
  equatable: ^2.0.3
  lint: ^1.7.2
  test: ^1.16.0
